<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mvhsapp.app">

    <permission
        android:name="com.mvhsapp.app.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.mvhsapp.app.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MVHSApp.Light">

        <activity android:name=".ui.AeriesActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MVHSApp.Light.WithNavDrawer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.SetupActivity" />

        <activity
            android:name=".ui.SettingsActivity"
            android:launchMode="singleTop" />

        <activity android:name=".ui.AboutActivity" />

        <activity android:name=".ui.MapActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MVHSApp.Light.Map">

        </activity>

        <activity
            android:name=".ui.CalendarActivity"
            android:launchMode="singleTop">

        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/map_api_key" />

    </application>

</manifest>
